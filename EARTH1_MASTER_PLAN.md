# EARTH-1 MASTER PLAN — The Road to Right Numbers
*Registered 2026-08-18. Direction: Pietro. Every mistake is a loss;
every benchmark is honest (held-out, preregistered); the deliverable
is a whitepaper demonstrating a model that WORKS.*

## The reframe this plan runs on

Every negative in the record is a teaching gap, not a machine limit:

| "The machine doesn't..." | The teacher that exists |
|---|---|
| population not load-bearing (rank-18) | INJECT real within-country structure: WVS7 microdata has ~290 variables × 97K respondents; GSS 50 years; ANES. Genesis learns per-country JOINT distributions (age × education × religiosity × values), not 18 means. |
| cohort gradients backwards (6/24) | FIT age-conditioning from the 1,860 official cells (already improving: 6→11/24 in the first hour) |
| event heterogeneity flat (rank −0.41) | FIT per-agent susceptibility on the six measured reaction cases + run-#8 data |
| temporal ≈ no-change | VERIFIED W6 truth (Pietro) + fitted cohort replacement + A6.2 on real data |
| LLMs beat us (claimed) | Measured FALSE live: engine 10.59 < naive 12.64 < Sonnet-5 16.6 |

## THE BENCHMARKS — current → target (all held-out, all preregistered)

| # | Benchmark (truth source) | Current | TARGET | Primary levers |
|---|---|---|---|---|
| B1 | Cross-sectional GOQA 40×66 (real, corrected) | 10.59 vs naive 12.64, 34/40 | **≤ 9.0, ≥ 36/40** | microdata-enriched genesis features; cohort-informed calibration; estimator refinements |
| B2 | Cohort/distribution (official W7 cells, NEW) | 21.7pp, gradients 6/24 | **≤ 8pp, ≥ 75% gradient direction** | fitted age physics (E1-0.5); cohort calibration (already +1.85pp) |
| B3 | Temporal W6→W7 (VERIFIED truth) | unmeasurable (truth error > signal) | **beat no-change with p<0.05 on verified deltas** | W6 microdata; fitted cohort replacement; A6.2 |
| B4 | Event heterogeneity (reaction cases + run #8) | rank corr −0.41 | **rank corr ≥ +0.5, aggregate stays ≥0.9** | fitted per-agent susceptibility |
| B5 | Polymarket/G4 record (resolving markets) | n=8, scoped Brier 0.43 | **n ≥ 30, scoped Brier ≤ 0.20, beat raw-LLM** | daily arming (live), anatomy-gated abstention, susceptibility |
| B6 | LLM standoffs (live, preregistered) | beat Sonnet-5 & Haiku | **hold vs GPT-5 + Gemini (slots reserved)** | keys (Pietro) |
| B7 | Pew Global Attitudes cross-check (independent of WVS) | not yet run | **beat naive on ≥ 60% of overlapping items** | Pew datasets are public downloads; corpus builder imported |

Targets are ambitions, not promises — but every miss must produce its
fix-attempt in the same session (the new operating rule).

## WORKSTREAMS

**W1 — Information injection (the big unlock).** Genesis v3: per-country
JOINT trait distributions fitted from microdata respondents (WVS7 now;
GSS/ANES for US depth; EVS/Pew added). Kills rank-18. Every added
dimension must pay rent on B1/B2 held-out. THIS is how the population
becomes load-bearing — agents finally carry information countries
alone cannot.

**W2 — Fitted physics (E1-0.5 registered build).** Age-conditioning +
cohort-replacement gradients fitted from official cells (replaces
authored _AGE_GRADIENTS); per-agent susceptibility fitted from
reaction cases; batch-rewire perf already done. Constants earned,
never authored — the measured law of this codebase.

**W3 — Truth completion.** W6/W5 microdata (Pietro registration —
THE gating external item); Pew download+aggregation; EVS cross-check;
all through the spot-check-gated pipeline. Every benchmark stands on
verified ground.

**W4 — The record.** G4/Polymarket daily accumulation to n≥30 (live,
supervised); run #8 (fires today); order-effect/framing data for the
Build-31 superposition test.

**W5 — The whitepaper.** Skeleton now; numbers as they land:
  1. The model (genesis from microdata, forces, response law)
  2. Cross-sectional results (B1 + leakage + ablation + attribution)
  3. Distribution results (B2 — the differentiator: nobody else grades
     WHICH people)
  4. Live standoffs (B6 with prereg receipts)
  5. Market record (B5)
  6. Temporal honesty (B3 + what only verified data allowed)
  7. Negative-results appendix — the credibility engine: eleven
     reviews, every kill, every correction. THIS is why buyers should
     trust the positives.
  Draft at week 6; numbers frozen week 8; close-ready week 9.

## SEQUENCE (next 14 days)
D1-2: cohort-cal 200K definitive → production path if it holds;
      age-gradient fitting from cells (first E1-0.5 constant);
      Pew download + first B7 run; run #8 lands.
D3-5: Genesis v3 prototype (one country deep: US from GSS+WVS joint
      distributions) → micro-test B1/B2 delta; susceptibility fit v1
      → B4 micro.
D6-9: Genesis v3 all-WVS7-countries; E1-0.5 registered build
      assembled (fitted constants + estimator + calibration changes),
      full battery, adopt or iterate.
D10-14: W6 data lands (assumes registration ~D1) → B3 first honest
      temporal number; whitepaper section drafts for everything banked.
Weekly: full benchmark battery re-run (18-min rungs make this cheap);
        one-page delta report — numbers must MOVE toward targets or
        the miss gets a named fix.

## Standing discipline (unchanged, now serving improvement)
Held-out always; preregister before result; misses recorded as
misses; supervisor runs everything; small-first; clock checked.
The referee stays honest so the wins are sellable.
