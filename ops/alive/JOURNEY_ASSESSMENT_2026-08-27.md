# JOURNEY ASSESSMENT — the two-day campaign to 0.9
(Full text delivered in chat 2026-08-27; this is the durable copy.)

START: Epoch 3 (v4.1) live; benchmarks A/B red (population worse than
independence, magnitudes mixed-sign wrong, cohort structure coin-flip);
world losing ~2%/yr population; inference architecture on paper only.

RULINGS ARC: PDF assessment → three-track GO (SBI twin ∥ C2+ ∥
governance, 4 amendments) → MAKE EARTH-1 WORK → MAKE EARTH-1 LEARN
(Experience Loop north star) → product pivot reversed same hour →
CALIBRATE FIRST under Bible v4.1 → BENCHMARK AGAINST REAL DATA.

BUILT: fail-closed data-role registry + licence audit; sealed-truth
blinding + hash-chained replayable experience ledgers; SBI stack
(screens/ABC/NPE/NRE/SBC); C2+ substrate (IPF tables, sex axis,
byte-identical default); Experience Loop harness (4 arms, shocks);
null_branch contrast contract; H_poverty operator (household-pooled);
fetched-anchor pipeline (WB/PIP, hashed, versioned); recorder v2 on
production; STATUS.md delivery mirror.

DISCOVERED: physics inferable from consequences (5/6 recovered, zero
false confidence; cascade cliff MEASURED); incumbent genesis worse than
independence (IPF +24.8% withheld joints); loop v0 NO-as-gated (naive
won on calm worlds — arena fixed in v0.1); THE COMPENSATING-ERROR
CHAIN: binary deprivation cliff (30.5% pinned at max, 0.5% in between)
masked by over-educated genesis; gradient repair moved poverty shape,
mortality (2.24→1.24%/yr untouched hazards), starvation and cascades
together; income calibration from fetched PIP median/skew produced an
UNTARGETED bullseye ($8.30 line 48.8% vs real 46.1%).

ISSUES→SOLVED: VOID discipline caught every instrument defect
(memory-probe amendment, fencepost world, desync trap); ops incidents
(deploy-pin crash-loop — guard worked, zero loss; pkill self-matches ×3
→ script-file+PID rule; .env credential exposure disclosed + rotation
advised); process failures corrected by founder (memory-typed anchors →
fetched-only; latent-vs-monetary category error → explicit operator;
"nothing to do while computing" → calibration sprint; swallowed chat
updates → STATUS.md).

NOW: candidate 0.9 real-anchored (median 1.00×, $8.30 1.06×, deaths
1.31%); 1,136 tests green; flags off by default; Epoch 3 untouched;
v0.1 + gain bank closing; A-v2 queued. Reds carried: $3.00 tail 1.83×,
unemployment ~2×, death-age −25 yr (hazard-structure, BLOCKED_ON_DATA
for age curves).

NEXT: tonight's three verdicts → 200k battery → A-v2 DEV → B DEV
retest → cascade regression → FREEZE 0.9 → Epoch 4; reds via ILO/UN
WPP fetches; gains re-evaluated under new physics; loop v0.2/v1 (state
assimilation, rolling origin); holdout spent once at the end; THEN the
validated base ships to product through the typed adapter.
