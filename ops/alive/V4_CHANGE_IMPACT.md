# V4 CHANGE IMPACT — blast radius of 0.8-candidate-v3 → v4 (posthumous invariant)

Analysis only (2026-08-23). v4 untouched. Source: `earth1/alive.py` diff 69ee9d0 → main, traced through the canonical call graph.

## 1. Changed paths

| # | change | state read | state written | downstream systems reached | locality of effect | accumulates? | frozen stages that depend on it |
|---|---|---|---|---|---|---|---|
| 1 | rows dead at tick start snapshot/restored (`_snapshot_deceased`/`_restore_deceased`): civ.forces/alpha/openness/doubt/desire_intensity/age + 26 life arrays | dead rows only | dead rows only (restored to tick-start values) | nothing living reads dead rows through the masked paths (#2–#5); unmasked readers that still see dead rows: `knowledge_tick` (adj@stock), `flourishing_tick` (adj@stock, art_flow), `institutions` (deg), `memory.Chronicle.spread` (adj of scope members), `life.py:668/776` social terms — they now see FROZEN instead of drifting dead values | local (dead rows); second-order on living via the unmasked neighbour means | no (dead values are constant); the set of dead rows grows with the standing dead fraction | A (health envelope via demographic turnover), C (census denominators — already alive-masked), I |
| 2 | `_living_view(civ.adj)` for `propagate` (partner sampling, dyadic move, encounter evidence) | adj, alive | civ.forces (living rows; dead rows restored) | every living agent's daily dyadic update; conviction evidence (#3) | population-level, proportional to (dead fraction) × (share of a living agent's tie weight into dead alters). Edges into dead alters are pruned by plasticity only when their tie type is rewritten, so before v4 a dead alter stayed a sampled partner for days–weeks | yes, with the standing dead fraction (365 d: ~1–2 %; Epoch 1 at day 3845: 17 %) | A (sdr, unanimity, alpha, sat), B3 (transmission), B7/B8 (cascade substrate), E, H (FSLE/butterfly/noise run through propagate), I |
| 3 | `update_conviction` on the living view (scratch evidence comes from #2's encounters) | scratch, alpha | civ.alpha | conviction → susceptibility-weighted moves next day | as #2 | as #2 | A (alpha_interior, softening), B2 detection instrument unaffected, E, I |
| 4 | trait feedback `dev = F − (adj_live@F)/deg` | adj, forces | civ.openness/doubt/desire_intensity (living) | traits → susceptibility, life targets over long horizons | population-level, small (residue 0.02 × neighbour-mean shift) | slowly, via traits | A (trait drift over 365 d), I |
| 5 | `feed_tick` on the living view of `w.feed` | feed matrix, alive | civ.forces (living) | media channel: a dead hub no longer broadcasts; dead rows no longer consume | local to hubs that die (rare) + their audiences | no | A, E (feed is one transmission pathway) |
| 6 | cascade detector: `met` and `pop_l` over living residents only | forces, alive, locality | chronicle.cascade_* (episodes, residues, cooldowns) → effective_forces overlay | cascade incidence, overlay exposure, IDENTITY displacement | locality-level; changes the hot fraction only where dead residents were material; dead rows under v3 relaxed like the living so they were a near-neutral dilution | with dead fraction | C (fires, hot set, exposure), B7, B8, D (overlay timescale), G, I |
| 7 | employment released at death (`employed/in_lf=False, firm=-1`) | alive, life | life (dead rows) | firm payroll counts (firm_health is an autonomous random walk — independent of employee count), rehome pools (already alive-filtered), journal/API unemployment (now living-only) | local | no | none of the frozen gates read dead employment; F/G receipts read living agents |
| 8 | (state only, proven bit-identical) person ids, partner, geography, recorder | — | — | none | — | — | none |

Unchanged paths (evidence on them inherits directly): life_force_target and relaxation law (GEO-1), dyadic law parameters (k, μ, tie weighting), conviction gain, Chronicle press/decay/rehearsal, cascade rules/thresholds/cooldowns/half-lives/episode-entry semantics, open-loop overlay and clip, plasticity, rebirth, health/weather/war/flourishing/knowledge/class/institutions (already alive-masked), persistence serializer (new fields ride along; bitwise round-trip tested).

## 2. Magnitude evidence so far
20k, day 200 (dead 0.62 %), 30 days paired: living stored forces |Δ| mean 0.0007, max 0.060; population means equal to 3–4 dp. Effect scales with the standing dead fraction; Stage A horizon (365 d) keeps it in the ~1–2 % dead regime; multi-year horizons (Stage I) reach the regime where it is material (Epoch 1: 17 % dead slots).

## 3. Dependency map to stages
- Touched by #2–#6: A (all four health gates are living-agent statistics whose inputs changed), B3, B7, B8, B12 (new restored state must serialize bitwise), C, H-FSLE, H-butterfly, H-noise (branch.run → propagate), E, D (overlay via #6), G, I.
- Not touched: B1, B2, B4, B5, B6, B9, B10, B11, B13 (their broken mechanisms and detectors do not pass through the dead/living boundary); H-consciousness (fresh 12k genesis, 25 days: expected deaths ≈ 12k × 25 × daily hazard ≈ single digits → dead fraction < 0.1 %; the change is below the instrument's resolution — INHERIT with that bound stated).
