# STAGE C SUB-REGISTRATION — eight-force + cascade census (frozen)

Methodology already locked in STAGE_CD_METHOD_LOCK.md (measure set,
descriptive bins 0.05/0.20/0.45, characterization-only gate policy).
This sub-registration fixes only the run parameters, before any
Stage C data exists:

- World: candidate v2 (76a574c lineage, three flags), natural
  no-news world, N = 200k, 365 days.
- Seeds: 9501, 9502 (fresh; never used).
- Census cadence: every 10 days, per the locked measure set:
  C_t = F_effective − F_stored decomposed by channel, rule,
  locality (top-decile concentration), cohort (income/age/urban),
  superposition count, residue age, clip occupancy (±0.5 and
  [0,1]), duration above the three descriptive bins, earthling
  exposed fraction, peak vs terminal, and instrument-recovery
  check (C is derived: expiry ⇒ immediate zero).
- Output: data/acceptance_0_8/stageC/. Deliverable: measured
  characterization report; NO gate is scored; any future
  effective-expression gate must be independently derived and
  preregistered before use (as locked).
- Stage outcome semantics: Stage C cannot FAIL on the overlay
  numbers themselves (characterization); it can only VOID on
  instrument defect. Its findings feed the founder's D2 question
  (are the legacy rules' natural firing rates/amplitudes plausible
  physics?) — any consequent change would be a new founder-
  authorized program, never a quiet adjustment.
