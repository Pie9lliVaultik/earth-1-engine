# 0.8 IT10 — HIGH-DOSE TWO-TIMESCALE INTEGRATION (pre-registered)

IT9 (9a7269b) preserved as VOID; its observations are hypotheses,
not wins. IT10 asks ONE question: does λ* adaptation supply
persistence inside the saturation-healthy IT6-ALL high-dose regime?

## The single scored candidate

IT10-CAND = exact corrected IT6-ALL system (k=3, μ=0.05,
relax=0.045, dyadic influence + dyadic conviction gain 0.003,
flourishing level-map, cascade cooldown, dyadic feed, contagion
ambient off, unchanged event magnitudes/targets/bounds) + the
accepted stationary-safe adaptation law
    b ← clip(b + λ*(F − T), 0, 1),  λ* = 0.1072 (frozen, IT9-derived)
applied daily to life.force_baseline, all channels. Nothing else.
No μ/relax/λ/event adjustments.

## Fresh scored evaluation

The combination was selected after observing IT9 ⇒ scored runs use
FRESH seed 8902 (never used anywhere). Historical seeds only for
non-scored continuity (KA0 at 8890).

## "Fast remains fast" tolerance (frozen from history, before any
IT10 result)

Live-force transient: tau within the standing [5, 15] band AND the
LIVE component of the day-30 residual (total residual minus the
baseline-carried part) ≤ 0.15 — from the measured fast-only
residuals (IT6-ALL 0.074; IT9-KA1 0.097) plus margin. A secretly
slowed field fails this even if total residual is in band.

## Repaired known-answer battery

KA0 flag-off continuity: IT6-ALL exact @ seed 8890 with λ=0 through
    the NEW code path must reproduce the recorded IT6-ALL metrics
    exactly (tau 5, resid 0.074, ring1 0.00702, sat 0.1601, sdr
    0.637, α 0.5113) — baseline machinery must not silently alter
    the base civilization.
KA1 λ=0 @ 8902: fast-transient/insufficient-residual behavior
    (resid < 0.2).
KA2 stationary invariance (EXACT semantic check, not empirical):
    construct F = T ⇒ Δb must be numerically 0.
KA2′ planted rejected law (guaranteed distinction, no asymptotic
    assumption): construct F = T with b ≠ T; accepted law adapts
    nothing; the planted (F−b) implementation must move b and be
    DETECTED as violating stationary invariance.
KA3 λ×10 sensitivity (local, not whole-world): at an identical
    interior state, one-step Δb(10λ*) = 10·Δb(λ*) within numerical
    tolerance (no clipping active); capture-speed comparison under a
    standardized 15-day sustained perturbation reported; long-run
    plateau diagnostic only.
Retained IT9 controls: near-stationary drift (control-world global
    mean |Δb| days 90→120 < 0.02); sign symmetry (±0.15 forks,
    |Δb+|/|Δb−| ∈ [0.5, 2]); transient-vs-sustained (15-day
    sustained exposure leaves ≥ 2× the durable baseline shift of a
    1-day impulse of equal magnitude).
Standing battery @ 8902 with λ*: zero/instant/pull/frozen/degtgt/
fastmix/ratchet/KAdis set.
Any required KA failure ⇒ VOID. Any substantive gate miss ⇒ FAIL.
No "almost pass".

## Scoring — the complete frozen joint gate, unchanged

tau [5,15] + resid [0.2,0.6] + live-residual ≤ 0.15 + registered
decay-to-plateau shape · sat < 20% every channel (the decisive
interaction) · sdr ≥ 0.5, unanimity < 50%, no railing ·
clustered ring1 [0.006,0.15], ring2 ≥ 0.0005, ring3 > 0, no
consensus explosion (diffuse + single-source diagnostics retained) ·
α interior/heterogeneous, dyadic softening/hardening, no
ratchet/freeze · lived-world causality intact · endogeny. Reported
separately, never collapsed: fast timescale; day-30 residual;
baseline displacement; fraction carried by baseline; recovery after
sustained-driver removal.

## Stop rule

FAIL → no search (no μ/λ/relax/event variants). Diagnose which
interaction failed: high dose suppresses adaptation capture;
adaptation memorizes bounded impulses faster than mixing diffuses
them; or baseline law couples to a remaining writer. VOID → repair
the instrument, as IT9 → IT10.

## If IT10 qualifies

Freeze the exact law immediately; no more force-physics tuning;
advance unchanged to the full acceptance chain (365-day endurance,
adversarial battery, census, two-timescale map, transmission
validation, India recession probe, opinion causal receipt, canonical
chaos remeasurement) + close the decay_half_life contract before
Epoch-2 eligibility. Production untouched.
