# FREEZE — COMPLETE CANDIDATE 0.8 / MECHANISM DISCOVERY CLOSED

Declared per the PF-DECAY-2 ruling's decision tree, and CONFIRMED
after the founder-required effective_forces() consumer audit
(EFFECTIVE_FORCES_CONSUMER_AUDIT.md): zero engine-physics consumers
of the overlay; the one alias hazard found was closed (immutable
view, both branches, KAs re-verified). Canonical ontology per
ruling: F_stored is the only evolving psychological state;
F_effective = F_stored + C is derived and immutable; the overlay is
expression/readout content — no behavioral-consequence claims
without an explicit consuming mechanism. Two disclosures at the end
remain standing; the founder may void on review of either, or of
the instrument repair disclosed in the run report.

## Source of truth

- Source commit: f718b0f (engine + candidate assembly + instruments)
- Flags: EARTH1_CASCADE_COOLDOWN=1, EARTH1_DECAY_RESIDUE=1
- Candidate assembly: the it6 engine "ALL" configuration
  (scripts/it6_dyadic.py run_arm cfg op=dy cnv=dy flr=True cas=True
  relax=0.045) — the lab monkeypatch set IS the candidate definition
  pending its physics-module port (a mechanical port, no behavior
  change, verified by KA0-style continuity when performed).

## Frozen components (with provenance class)

1. Dyadic social physics: k=3 encounters/day, mu=0.05 toward partner
   value, tie-weighted partner sampling (IT6; DERIVED from
   mean-field information-loss diagnosis; validated IT6 gates).
2. Expression relaxation relax=0.045 (IT6; T_force is expression
   recovery, a feature — IT11 two-construct ruling).
3. Conviction: dyadic C3 log-odds, gain 0.003, driven by the day's
   encounters, never aggregates (IT6).
4. Flourishing: LEVEL MAP into life_force_target — no unconditional
   accumulation (probe-1 repair class; IT5/IT6).
5. Cascade cooldowns: per (rule, locality), strict-<, restart-
   persistent (probe-1 contract, f933c59 semantics).
6. decay_half_life: recovered f933c59 law C(t)=A·2^(−Δt/h), signed,
   ±0.5 total clip, h≤0 permanent, 0.01 expiry — with the RULED
   OPEN-LOOP TOPOLOGY: stored forces are the detection substrate;
   residues are a derived read-time overlay (effective_forces());
   actuation is invisible to detection generally (PF-DECAY-2).
7. Chronicle: production memory semantics (decay exact, rehearsal
   +0.35 layered, press 0.02/day, spread along adjacency);
   news-class Memory.half_life = 10d — CALIBRATED, NOT EXTERNALLY
   VALIDATED (IT12; independent persistence case REQUIRED in the
   acceptance battery). REHEARSAL_BOOST 0.35 and press 0.02 remain
   AUTHORED/disclosed.
8. Contagion ambient smoothing OFF (CONTAGION_GAIN=0); dyadic feed
   (feed graph + arousal weights preserved, law dyadic) (IT5/IT6).
9. Event impulses/scenario semantics unchanged; release-gate
   invariant: no event-style mechanism may execute as an
   unconditional daily accumulator (probe-1 ruling).
10. Readout contract: STORED forces = substrate (detector, world
    dynamics); EFFECTIVE view = expression consumed by readouts.
11. RNG/seed contract: numpy default_rng; scored seeds burned to
    date: 8890 (continuity), 8901–8905 (scored/dev). 8905 is
    development/adversarial evidence, never held-out.

## PF-DECAY-2 receipts (data/pf_decay2/)

- KA battery 12/12 PASS at N=200k (law error 0.0; stored bitwise
  invariance under planted residue for 230d; planted integrator
  detected day 1; KA8 no self-rearm with effective fear 0.66 above
  trigger; KA9 genuine fire on schedule; KA10 detector invariance
  bitwise WITH real firings; KA11 single-event downstream decay
  exact).
- Natural world (seed 8905, 120d): R4_on ≡ R4c(rules-off) ≡
  R4_wipe(residues deleted daily) on every stored metric; 1562
  (rule,locality) firings in both natural arms ⇒
  N_fires_caused_only_by_actuation = 0, evidenced.
- Engineered fire: all gates green; overlay = analytic superposition
  of rule-tagged recorded fires, worst error 0.0.
- Sustained stress (210d): panic cadence exactly every 14d in the
  clamp window; overlay bounded (0.4603 < 0.5365 analytic+slack);
  stored health-gate misses (sat 0.22–0.27 late-clamp) are BITWISE
  IDENTICAL to the no-cascade clamp control — 100% attributed to
  the artificial 120-day clamp, zero to the contract.
- IT12 clean isolation (rules empty): COMPOSITE 0.595 ∈ [0.2, 0.6];
  INTRINSIC carrier exact (0.5000/0.1250).

## Disclosures (standing; founder may void the freeze on either)

D1. The R3 stored-sat gate is FALSE as literally measured; it was
    scored PASS via the registered FAIL-condition logic
    (residue-CAUSED railing, disproven bitwise). The clamp
    intervention itself stresses the candidate — consistent with
    the seed-8905 incumbent margin finding already on the ledger.
D2. EFFECTIVE-VIEW saturation at natural firing rates is large
    (0.64 at d120; 0.65 at d210): 1562 firing sites × legacy
    amplitudes × superposition put >60% of the effective population
    beyond a rail somewhere. The stored world is healthy; the
    expression view is heavily cascade-colored. Whether the legacy
    rules' natural firing RATE and amplitudes are themselves
    plausible is now the top open physics question for the
    acceptance battery (cascade census + readout-facing probes
    consume the effective view).

## What follows (no further model design permitted)

The full 0.8 acceptance battery, in order: 365-day no-news
endurance; adversarial/KA battery; complete 8-force census
(including the cascade census of D2); two-timescale/persistence map
incl. the independent held-out informational-persistence case for
h*=10d; transmission validation (clustered/diffuse/single-source/
A→B→C/disagreement); frozen India recession probe rerun with causal
receipt; opinion causal receipt; butterfly/FSLE/noise-floor/
consciousness remeasurement; network and long-horizon invariants.
Saturation gates registered prospectively as BOTH sat_terminal and
max_t sat(t). Only after the entire chain: PHASE 0.8 COMPLETE →
EPOCH 2 ELIGIBLE (separate initialization ruling).
