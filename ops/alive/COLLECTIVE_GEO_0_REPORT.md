# COLLECTIVE-GEO-0 — DIAGNOSIS COMPLETE

Read-only; no parameters touched. Instruments: genesis measurement
(N=20k), full 120d candidate world (N=25k, dev seed 9001), exact
term-by-term target decomposition (residual 0.0000), dyadic
ablation. All numbers below are measured, not read from code.

## Answer: why does no-news Earth-1 want COLLECTIVE ≈ 0.885?

GENESIS IS INNOCENT. Day-0 COLLECTIVE: mean 0.630, p95 0.871,
0.9% above 0.95 — empirically grounded ((1−openness)·0.6 +
Hofstede-PDI·0.4 + region deltas; openness mean 0.437 from Big-Five
+ education/age structure). Healthy geometry at birth.

THE TARGET LAW LIFTS THE WORLD +0.23 AND PARKS IT THERE. The
population migrates 0.63 → 0.86 over ~100 days (relax 0.045 chasing
a high target — which is why every 120-day window looked fine) and
equilibrates at F ≈ T. Exact decomposition of the equilibrium
target (population means):

    base (birth forces)                 0.633
    + 0.40·dep·shared                  +0.040   (dep .29, shared .28)
    + 0.25·political − 0.20·social_need+0.071   (political .40 → +.100;
                                                 social_need .075 → −.015)
    + 0.20·belonging (flourishing map) +0.115   (belonging .73)
    ────────────────────────────────────────────
    T                                   0.859   (measured F: 0.865)

THE SMOKING GUN: frac(T > 0.95) = 41.8%. The target distribution
itself parks 42% of humanity above the rail-line (clipped at 1.0);
stored forces follow (47% above 0.90), and ordinary breathing
pushes the >0.95 stored tail past 20% at year horizon.

SOCIAL DYNAMICS EXONERATED: dyadic influence OFF for 15 days moves
the mean 0.867 → 0.869 — a preserver, not a pusher. Cascades,
diversity, drift, clipping already excluded in the Stage A report.

## Classification (founder's taxonomy)

Primarily (6) with a strong component of (5):
(6) individually-reasonable writers whose SUM is unreasonable —
each modifier is a defensible structural claim (shared hardship,
political engagement, belonging), but their sum was never audited
against the genesis geometry it feeds on;
(5) accidentally biased target function — the COLLECTIVE target's
sign structure is asymmetric: three substantive positive terms
(+0.04, +0.10, +0.115) against two negligible negative ones
(social_need −0.015 at mean 0.075; addiction ≈ 0 at mean 0.014).
The channel has no effective downward writer in a neutral world.
Sub-provenance: the 0.25·political coefficient AND the political
distribution (Beta(2,3), authored at life.py:338) are class (3)
authored constants; the 0.20·belonging coefficient entered via the
IT5-era flourishing level-map conversion (authored scale, chosen to
convert the old increments, never audited as a level).

## The evidence-identified property (named, not proposed)

In a neutral healthy world the target law should reproduce the
empirically-derived genesis geometry it is anchored to; instead it
adds ≈ +0.23 unconditionally, relocating a calibrated 0.63
distribution to within one breathing amplitude of the pole. Any
GEO-1 repair (founder authorization required) would need an
independent-evidence derivation of the COLLECTIVE modifier scales —
e.g., a neutral-world invariance criterion (T(neutral) ≈ genesis
distribution, with modifiers expressing DEVIATIONS from neutral
rather than unconditional additions) — not a coefficient search
toward "sat < 20%". Per ruling: no grid fishing; smallest
evidence-derived bracket; regression battery (IT6/IT12/PF-DECAY
invariants + known-failure seeds 9001–9003 as development controls)
before any refreeze; then Stage A on fresh seeds.
