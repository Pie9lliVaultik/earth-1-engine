# 0.8 IT7 — SINGLE-FACTOR PERSISTENCE CONFIRMATION (pre-registered)

IT6 (3442ddd) accepted: 0/21 under frozen criteria, unedited. IT7
changes ONE factor in the exact IT6 ALL candidate.

## Admissibility chronology (why this is not coefficient fishing)

1. IT4-v2 (BEFORE IT6) measured the persistence-compliant region:
   relax 0.01–0.02 → resid_d30 0.203–0.205 (in band), and IT6's own
   KA_frozen (relax 0.005) showed resid 0.223 — the region is
   bracketed by pre-existing measurements on both sides.
2. IT6 registered relax=0.045 in the candidate (registration error).
3. IT6's ALL then passed every substantive joint requirement —
   transmission through ring 3, no consensus explosion, all channels
   under saturation, interior heterogeneous conviction, diversity —
   except the persistence residual (0.074 vs [0.2, 0.6]).
4. IT7 is therefore a bounded confirmation of an already-measured
   region, not a post-hoc search.

## Arms

Candidates (scored): ALL@relax=0.02, ALL@relax=0.01.
Reference (instrument continuity, NOT scored): ALL@relax=0.045
(must reproduce IT6's numbers).
Everything else byte-identical to IT6's ALL: dyadic operator
(k=3, μ=0.05) on ties+feed, dyadic conviction (C3 log-odds,
gain 0.003, same-encounter evidence), flourishing level-map, cascade
cooldown, contagion ambient off, population 200k, seed 8890, 120
days + day-90 fork, clustered seeding, same metrics, same acceptance
thresholds. No additional relax values after results.

## Known-answer battery

Carried forward at relax=0.02: zero-encounters, instant-consensus,
excessive pull (0.60), frozen field (0.005), degenerate targets,
fast-mixing graph, incumbent-ratchet conviction.
KA_mfdrive is MARKED INSTRUMENT-INVALID prospectively (IT6 finding:
natural encounter distance ≈ 0.20 under the frozen construct ⇒
natural negative drive ≈ 0; the plant is undetectable by a natural-
softening collapse that never exists). Not retroactive.
REPLACEMENT designed-disagreement controls (drive-level exposure on
10k-agent cohorts, law unchanged — probe-3 methodology at encounter
semantics):
- KAdis_soften: cohort drive forced to −0.5 → the dyadic law must
  soften (P(Δα<0) ≥ 0.9, dose-consistent);
- KAdis_harden: cohort drive forced to +0.5 → must harden;
- KAdis_mf: same designed exposure but conviction computed from the
  NEIGHBORHOOD-MEAN construct → the disagreement signal must be
  destroyed (cohort does NOT soften) and thereby DETECTED as
  different from KAdis_soften.
Natural disagreement prevalence remains a reported ecological
observable with NO population-fraction requirement. Registered
ecological note: even opposite-tail real-agent pairs have mean
8-channel distance ≈ 0.35 < 0.5 — genuine-disagreement exposure is
essentially nonexistent among real agents under the frozen
construct; a future-construct question, recorded, not compensated.

## Mechanical decision

Neither candidate qualifies → STOP; no further relax search;
diagnose why IT4-v2 persistence evidence failed to transfer.
One qualifies → it advances. Both qualify → registered tiebreak:
resid_d30 closest to band center (0.4); secondary: higher clustered
ring-1. The chosen law is then FROZEN — no further tuning — and
advances unchanged to: 365-day no-news battery; adversarial/KA
controls; full force census; tau map; clustered+diffuse+single-
source transmission; frozen India recession probe; opinion
causal-receipt retest; canonical butterfly/FSLE/noise-floor. Only
after that chain does it become Epoch-2 eligible. Production
untouched; Epoch 1 remains the pathological reference era.
