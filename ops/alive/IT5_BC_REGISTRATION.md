# 0.8 IT5-B + IT5-C — frozen registrations (pre-results)

Founder ruling on IT5-A (61cab68): flourishing contradiction repair
AUTHORIZED; IT5-C scope = the full continuous-influence operator
family (propagate + feed + contagion:210 smoothing); event-gated
impulses excluded. Frozen before any candidate result exists.

## IT5-B — conviction gain, derived then frozen

Preserved discovery (not re-litigated): C3's law form — agreement
hardens, disagreement softens, log-odds space, no absorbing bounds.
Under test: the MAGNITUDE.

Derivation (from already-frozen evidence, not simulation): attitude
certainty consolidates over months-to-years (registry T4: latent
attitude stability r 0.55–0.85 over 2–4y; the attitude-strength
literature places certainty consolidation on month+ scales, not
weeks). Rule: sustained near-full agreement (drive (agr−0.5)·2 ≈
+0.5) should traverse Δα ≈ 0.4 (0.5→0.9) in T_hard ∈ [90, 365]
days ⇒ gain = 0.4/(T_hard·0.5) ∈ [0.002, 0.009].

Frozen candidates: gain ∈ {0.009 (T≈90d), 0.003 (T≈270d),
0.001 (T≈800d — deliberately too-slow control: must FAIL the
softening/responsiveness criteria)} + incumbent 0.06 as pathological
reference. Anchor treatment: none (C3 has no anchor). Expected
timescales registered: hardening 0.5→0.9 in [90, 365]d under
sustained agreement; softening measurable within [30, 120]d under
sustained disagreement. Failure thresholds: softening fraction ≥ 10%
(day 60→90 window), α mean ∈ [0.2, 0.9], sd ≥ 0.05 at day 120 —
unchanged from IT4-v2. No post-result widening.

## IT5-C — the continuous-influence operator family

Scope: ONE candidate law governs propagate (friend/tie influence),
feed (media influence — echo-chamber topology, arousal weights, and
exposure structure PRESERVED; only the influence LAW under test),
and contagion:210 (disabled in operator-fix arms for causal
attribution; production change must be separately earned).
Event-gated impulses untouched.

Candidate families:
- F0 incumbent dense mean-field pole law (pathological control);
- F1 SPARSE DYADIC ENCOUNTERS: each agent processes k encounters/day
  (k ∈ {1, 3}), partner drawn tie-weighted from their row (feed
  exposure: one feed item/day from the feed graph with arousal
  weighting); per-encounter influence μ ∈ {0.05, 0.15} toward the
  PARTNER'S VALUE (not pole), susceptibility-scaled. Signal can hop:
  A→B today, B→C tomorrow.
- F2 = F1 + bounded-confidence gate (only encounters with
  |Δf_channel| ≤ 0.5 influence that channel) — tests gating on top
  of the dyadic form.
Registered budget discipline: report mean |Δf|/day from the social
family per arm; candidate budgets must land ≤ 0.02 SD/day (frozen
IT3 bound), and F0-vs-F1 comparisons cite budget alongside form so
"wrong form" is distinguishable from "too much dose".

## Flourishing contradiction repair (authorized arm)

Conversion registered NOW: the five increments become LEVEL
contributions inside life_force_target — target += {0.20·meaning →
CULTURE, 0.20·belonging → COLLECTIVE, (0.20·hope + 0.15·curiosity −
0.25·need) → DESIRE, (0.30·need − 0.20·hope) → FEAR, 0.10·curiosity
→ EXPERIENCE}, clipped as usual. REGISTERED ASSUMPTION: increment
coefficients are reinterpreted as level coefficients (bounded ≤0.30
shifts — plausible magnitudes; their calibration remains open, the
STRUCTURE is what this repair fixes). Ablations: (1) incumbent
accumulation, (2) flourishing force-writes disabled, (3) level-map
conversion — distinguishing "removed a pathological writer" from
"restored the intended mechanism".

## The joint program (runs after this freeze)

Base configuration for all "fix" arms: relax = 0.045 (earned
persistence region), conviction C3 at the IT5-B mid candidate
(gain 0.003) unless the arm varies it, flourishing level-map unless
the arm varies it, operator F1(k=3, μ=0.05) unless the arm varies
it, contagion:210 disabled in operator-fix arms.

Causal arms (registered minimum): incumbent; flourishing-fix only;
operator-fix only; conviction-fix only; flourishing+operator;
flourishing+conviction; operator+conviction; ALL THREE. Plus
variants: all-three with F1(k=1, μ=0.15), F1(k=3, μ=0.15), F2, and
IT5-B gain 0.009/0.001 swaps ⇒ ~14 experimental arms. Known-answer
arms: incumbent triple (in-grid), zero-influence (k=0), instant
consensus (μ=0.9, k=10), overpowered pull (relax=0.6), frozen field
(relax=0.005), anchor-frozen/ratchet conviction (0.06 ref in-grid;
gain=0 arm), degenerate-target (pole-biased synthetic lived state),
fast-mixing graph. VOID if any KA passes.

Acceptance: unchanged from IT4-v2 (persistence tau [5,15]d resid
[0.2,0.6]; ring-1 [0.006,0.15] AND ring-2 ≥ 0.0005; diversity
sd_ratio ≥ 0.5, unanimity < 50%, sat < 20%, no railing trend;
conviction heterogeneity + softening ≥ 10%; operator alive with net
contraction ≤ 2%/day) PLUS: lived circumstances still matter (force
response to a material-state delta ≠ 0), media exposure still
matters (feed-on vs feed-off differ measurably), flourishing still
matters (level-map arm differs from disabled arm). Zero qualifying
joint systems → STOP, no widening, diagnose. Production untouched;
Epoch 2 forbidden until the full battery + probe retest pass.
