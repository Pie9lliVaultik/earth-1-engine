# 0.5 PRODUCTION ACCEPTANCE — one production Earth

**2026-08-20 · VERDICT: ACCEPTED**

> Every mounted product call, branch, forecast and production readout
> refers to the same living civilization. No alternative Earth is
> accidentally reachable.

Each clause of that sentence is now machine-proven, not asserted.

## A. Plasticity production observation (days 1132–1143, restart inside)

| observation | value | note |
|---|---|---|
| strengthened/day | 11.56M ± 47k (0.4% CV) | stable regime; recorded, NOT judged — 0.8/Benchmark D owns magnitude |
| pruned/day | ~2.9k | 790k first-day transient settled immediately |
| rewired/day | ~19.0k | flat |
| tick spacing | min 59 / med 60 (max 240 = the restart's own 4.5 GB reload) | inside budget; 4M compute 46.3 s measured on prime |
| friends structure | 34.4M edges, degree mean 8.6 / p99 15 / max 24 | bounded, no runaway |
| weak structure | 1.33M edges | **heavy erosion from genesis — bridge-thinning trend flagged as OBSERVATION for 0.8/Benchmark D; not tuned** |
| weights | within [floor, max(genesis-max, 2.0)] | law bounds hold |
| corruption | 0 edges to dead agents; mutuality exact; no self-loops | |

## B/C. Restart + one-Earth smoke (real day-1142 4M snapshot)

Restart: `rng_continued True · dirty False · boundaries unchanged (2)` —
and **plasticity resumed from the persisted graph** (no re-transient;
a reinitialized fabric would have re-shown the 790k prune spike).

Smoke (in-process, live snapshot): canonical identity on every
response (`day 1142 · sha 379212b2…`); 6 read-only routes leave world
AND RNG bit-identical; `/ask` + legacy forecast fail loudly 503, never
legacy; `/world/tick` 410 (daemon sole writer); branch clone carries
every persistent field, shares no arrays, and one branch-day leaves the
parent hash-unchanged.

## D. Gates

Full CI 1065 passed · release gate ELIGIBLE · one-production-earth AST
gate 0 violations · legacy-import sabotage control proven in CI.

## Inventory reconciliation

47/47 pre-0.5 handlers accounted (⊇ 31/31 world-touching): 9 canonical
readouts · 1 canonical branch · 9 fail-loud-never-legacy · 15
intentionally retired · 13 DB utilities. Ledger: 1 PORT (delivered,
observed) · 2 READOUT-ADAPTER · 2 SUPERSEDED · tie-type ownership
closed, no unowned type.

**The Two-Earth problem is dead at the product boundary.**
Next: 0.6 kills the third world.
