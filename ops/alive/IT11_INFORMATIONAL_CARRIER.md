# 0.8 IT11 — INFORMATIONAL PERSISTENCE CARRIER (pre-registered,
with the four founder amendments)

IT10 (23635f3) preserved as FAIL; force-baseline integration
REJECTED as the general informational-persistence carrier. IT11
tests the architectural hypothesis (hypothesis, not conclusion):

    material history → material state
    informational history → memory/Chronicle
    current psychology → fast force expression

## GATE 0 — executable Chronicle census (from code, frozen)

Memory state: {id, label, day, force_signature(8), scope(bool/agent),
salience, half_life, rehearsals, origin}. half_life IS consumed:
salience ×= 0.5^(dt/half_life) exactly (memory.py:79). Press:
forces[scope] += salience·signature·0.02·dt, clipped — inside
live_one_day step 6 (chronicle.tick). Rehearsal: cosine>0.8 news
boosts old salience +0.35 AND appends the new memory (layering by
design). Spread: scope grows along adjacency (Chronicle.spread,
world-rng). Scope: boolean per agent. Restart: salience is the aged
state; chronicle is a PERSISTENT_FIELD → exact restart (KA5
verifies). Duplication: ingestion writes ONLY via
chronicle.remember(event_from_news(...)) — no parallel direct force
impulse for the same event; DOUBLE-COUNT GATE CLEAN (world_alive.py
:204-213 traced). Production DEFAULT_HALF_LIFE = 720 days —
AUTHORED constant (docstring rationale, no empirical source) →
amendment-1 classification: authored/unproven. A pass validates the
CARRIER ARCHITECTURE, not this number.

## GATE 1 — T1 construct audit (frozen)

T1's evidence (dynamic-persuasion decay: ~50% at ~10d, plateau to a
month) measures persistence of attitude effects AFTER informational
exposure — the message is gone; memory/attitude persists. No T1
source measures intrinsic state relaxation with no carrier. Verdict:
TWO constructs — T_memory (informational persistence; owns the
[0.2, 0.6] day-30 band) and T_force (expression-layer relaxation;
retained as an UNTARGETED diagnostic; its speed is potentially a
feature). The raw-force probe was construct-invalid FOR T1
specifically; IT1–IT10 results stand as what they measured.

## Registered analytic expectation (pre-run, honest)

0.5^(30/720) ≈ 0.971: the production half-life is expected to
OVER-PERSIST the band → the ruling's FAIL→calibration path. Scored
as measured; expectation recorded so a FAIL cannot be presented as
surprise, nor a pass as retrodiction.

## Candidate + canonical event (frozen)

Base = IT6-ALL exactly (k=3, μ=0.05, relax=0.045, dyadic
influence+conviction, flourishing level-map, cascade cooldown, NO
baseline adaptation), fresh scored seed 8903. At day 90 insert ONE
canonical informational event via the production mechanism:
Memory(force_signature: FEAR=+0.50 (others 0), salience 1.0,
half_life = 720 (production, the ONLY scored value), scope = the
clustered-locality 5k cohort, origin "scenario"), full Chronicle
semantics (decay+rehearsal+spread) for the scored arm. Paired
no-event control world (same seed/stream). 30-day window.
Measured separately every day: CARRIER (salience, scope size,
rehearsals, spread) and EXPRESSION (cohort FEAR delta vs control,
press contribution, decomposition), plus transfer ratio and (KA1)
post-removal recovery.

## Known-answer battery (amendment 2: KAs test what the equation
guarantees)

KA0 continuity: no event ⇒ reproduce IT6-ALL exactly (recorded
   metrics) — the harness changes nothing.
KA1 carrier removed: identical exposure, memory deleted after day
   1 ⇒ cohort force reverts on the fast-expression timescale.
KA2 decay disabled (half_life=∞): CARRIER magnitude must not decay
   (carrier-scored; no whole-field expectation).
KA3 analytic decay (exact): isolated memory, rehearsal+spread off:
   one-step salience ratio = 0.5^(1/720) to numerical tolerance;
   N-step composition exact.
KA4 scope: spread disabled ⇒ scoped cohort pressed, disjoint cohort
   NOT directly pressed.
KA5 restart: save/load mid-decay ⇒ identical continuation
   (deterministic tolerance).
KA6 sign: FEAR=−0.50 memory presses downward (signatures are signed
   reals; symmetric semantics documented).
Ablation attribution arms (diagnostic, not tuning): decay-only /
decay+spread / full Chronicle — so measured persistence is
attributed to intrinsic decay vs rehearsal vs social recurrence,
reported as such.

## Health + separation requirements

All IT6 gates stay frozen (sat/sdr/unanimity/α/KAdis/rings). The
dyadic ring-1/2/3 transmission claim is measured on its own probe
and may NOT be satisfied by Chronicle spread — different causal
pathways, reported independently. decay_half_life (cascade) stays a
SEPARATE unresolved contract — not merged, not bundled.

## Decision (with amendment 1 and 4 language)

Any required KA fails → VOID. Chronicle semantics ≠ carrier
hypothesis → STOP (no Chronicle redesign inside IT11). Production
half-life misses the band → IT11 FAIL; half_life becomes a
calibration parameter with a prospectively derived candidate in a
separate registered study. Carrier lands in band + civilization
healthy → the registered conclusion, precisely: "The raw-force
perturbation was construct-invalid for T1. Informational persistence
resides in the Chronicle path (mechanism validated); the production
half-life remains an authored parameter requiring provenance before
V1. Raw force relaxation measures expression-layer recovery
(T_force), a distinct construct."
