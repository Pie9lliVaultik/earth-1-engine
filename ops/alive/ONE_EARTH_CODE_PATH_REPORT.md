# ONE-EARTH CODE PATH — PROGRAM 3 COMPLETE (Bible 0.4/0.5 code-path)

Commits c54f444 → e066de7 → 5e323e1 (origin/v1-unification, tree
clean). Machine-verifiable report: `data/one_earth_routing_report.json`
(regenerated by `scripts/one_earth_routing_report.py`, wrapped as
`tests/test_answer_living.py::test_one_earth_routing_report_passes`).

## Status wording (founder-locked distinction)

    0.4/0.5 CODE-PATH COMPLETE ................ YES (this report)
    0.5 LIVE-PRODUCTION ONE-EARTH INVARIANT ... PENDING — CCX33 still
        runs the Epoch-1 executable under the founder ruling (no hot
        swap); completes only when the accepted Epoch-2 executable is
        deployed as the sole canonical Earth and every production
        surface resolves its UUID/hash and PHYSICS_VERSION.

## What is now true in code

1. Living answer path: `earth1/answer_living.py` — the one supported
   answer path; consumes only `alive.World` (expressed field via
   `effective_forces`, living life/material/social cohorts); no
   `earth1.tick`, bare `Civilization`, or `LivingWorld`. `answer.py`
   RETIRED → `legacy_answer.py` (opt-in guard). End-to-end demonstrated:
   50 registered questions answered from a living world with
   provenance; calibration status carried as UNCALIBRATED until
   Benchmark A (Phase 1).
2. Official benchmark: `earth1/benchmark_living.py` +
   `scripts/benchmark_living.py` — one ontology, full provenance.
   `benchmark.py`/`predictions.py` → `legacy_benchmark.py` /
   `legacy_predictions.py`, LEGACY_COMPARISON_ONLY with an ImportError
   guard (EARTH1_LEGACY_COMPARISON=1) — impossible to select by
   accident; question data extracted engine-free
   (`benchmark_questions.py`).
3. Product/API ontology: static proof for /ask, /world, /forecast,
   /observatory, /predictions(DB-only), api.deps, branch, timeline,
   assimilation, observe, observer, answer_living, benchmark_living,
   the daemon and the Observatory server: each resolves `alive.World`
   (direct/transitive/pure-consumer) and imports nothing quarantined.
   Runtime proof in a controlled canonical deployment: all surfaces
   return the same world hash, the same snapshot sha256 and the same
   PHYSICS_VERSION (`0.8-candidate-v2/76a574c-canonical`).
4. Quarantine: `field_lab`/`conviction_lab`/`propagation_lab` →
   `earth1/lab_archive` (PROVENANCE ONLY, import guard); IT6/IT11
   runners are thin canonical runners; the lab branches are retired.
   One authoritative implementation remains.
5. Dead-family invariant: `legacy_gate.QUARANTINED` now also covers
   the legacy_* modules and the lab archive; production and official
   benchmark paths enumerated; sabotage control proves the gate can
   fail; release gate structurally eligible; 0 violations.
6. Physics commitment identity: PHYSICS_VERSION in API identity,
   manifest (`physics_identity()`), and every persistence save blob;
   no environment variable selects physics (EARTH1_TEST_* twins only).
7. Stage C E3 DIAGNOSTIC CONCERN (cascade/IDENTITY) untouched and
   ledgered; canonical code reproduces it by construction (bitwise
   equivalence). Resolution follows on the unified implementation via
   the Bible miss protocol — next scientific issue, not this program.

## Evidence

Full test estate 1088 passed / 6 skipped; `python -m earth1.release_gate`
structurally eligible; `python -m earth1.legacy_gate` clean;
`ONE_EARTH_CODE_PATH: PASS`.
