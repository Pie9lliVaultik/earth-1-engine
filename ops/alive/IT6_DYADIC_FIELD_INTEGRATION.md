# 0.8 IT6 — DYADIC FIELD INTEGRATION (pre-registered, frozen)

Founder ruling on the residual probes (1684289): the unifying defect
is that mean-field aggregation destroyed the causal unit — it diluted
minority transmission and hid individual disagreement. IT6 restores
the encounter as the unit of social causation:

    A encounters B → B changes → B later encounters C

IT5 remains FAILED under its frozen criteria and is not edited or
rescored. Frozen BEFORE any candidate result.

## Candidate system (nothing else added)

1. earned persistence regime: relax 0.045, weak influence budget;
2. dyadic influence (k=3 encounters/day, tie-weighted partner,
   μ=0.05 toward partner value) replacing mean-field propagate;
3. DYADIC CONVICTION: hardening/softening driven by the SAME
   encounters that carry influence — never recomputed from any
   aggregate. Per encounter: drive_e = (0.5 − d_e)/0.5 clipped to
   [−1, 1], d_e = pre-interaction mean |Δf| vs the partner; the
   day's drives accumulate and update α once (C3 log-odds form,
   gain 0.003 evidence-derived);
4. flourishing level-map conversion (accumulation contradiction
   repair);
5. cascade cooldown repair (EARTH1_CASCADE_COOLDOWN=1);
6. feed governed by the same dyadic law on its own topology and
   arousal weights (one item/day); feed encounters contribute
   conviction drive identically; contagion ambient smoothing off in
   corrected arms (uncalibrated-family attribution stands).

## The encounter as causal object (contract)

Every encounter carries: source, recipient, layer (tie/feed),
pre-interaction force distance, signed drive, force update, its α
contribution, strength (μ·susceptibility), day. Full records for a
registered 1,000-encounter/day sample per arm; aggregates
(encounter counts, drive distribution, disagreement rate) for all.

## Softening — new prospective registration (replaces the invalid
passive criterion; nothing rescored retroactively)

Two quantities, registered separately:
- CAPABILITY: P(Δα < 0 | net-negative daily drive) ≥ 0.9 and
  dose-responsive magnitude; hardening symmetric under net-positive
  drive.
- NATURAL PREVALENCE: the measured fraction of encounters with
  drive < 0 and of agents with net-negative daily drive — REPORTED,
  with no arbitrary population-fraction requirement. If natural
  disagreement is ≈ 0 even under dyadic encounters, that is a
  social-network/exposure finding recorded on its own — never
  compensated by artificial disagreement.
- Field health: α interior/heterogeneous (mean [0.2, 0.9],
  sd ≥ 0.05, <20% beyond either extreme), no ratchet, no
  anchor-freeze.

## Transmission test (both geometries; physics vs exposure kept
distinct — clustering is a TEST protocol, never hard-coded into
Earth-1)

Day-90 fork, all arms: CLUSTERED seeding (one large locality,
primary probe). For the full candidate and the incumbent: also
DIFFUSE seeding (diagnostic, reported not gating) and a
SINGLE-SOURCE path test (one seeded agent, 500-agent neighborhood
watch, mechanism visibility). Measure rings 1/2/3. Gate: clustered
ring-1 ∈ [0.006, 0.15], ring-2 ≥ 0.0005, ring-3 > 0; no consensus
explosion (unanimity < 50%, sd_ratio ≥ 0.5 maintained).

## Ablation matrix (causal attribution mandatory)

incumbent · dyINF only · dyCNV only (encounter-sampled drive with
mean-field influence retained) · FLR only · CAS only ·
dyINF+dyCNV · dyINF+FLR · dyINF+CAS · dyCNV+FLR · dyCNV+CAS ·
ALL − FLR · ALL − CAS · ALL (full candidate). No improvement is
credited to the candidate unless the ablations locate its source.

## Known-answer arms (must all fail in their registered directions;
any KA passing ⇒ VOID)

incumbent triple (in-matrix) · zero encounters (k=0) · instant
consensus (k=10, μ=0.9) · mean-field conviction drive planted
inside the full candidate (the probe-3 pathology must be detected:
natural softening collapses) · overpowered pull (relax 0.6) ·
frozen field (relax 0.005) · degenerate targets (t≡0.9) ·
fast-mixing randomized graph.

## Hard acceptance (ALL simultaneously; frozen bands unchanged)

persistence tau [5,15]d resid [0.2,0.6] · clustered transmission
gate above · diversity (sd_ratio ≥ 0.5, sat < 20%/channel,
unanimity < 50%, no railing trend) · conviction (interior,
heterogeneous, capability + symmetric hardening, no ratchet/freeze)
· lived-state coupling alive (material Δ moves forces) · flourishing
matters without accumulating (level arm ≠ disabled arm; no monotone
drift) · cascades bounded (cooldown semantics active; no daily
grinding) · 365-day endogeny for the winner (battery stage). Zero
qualifiers → STOP, no widening.

## If IT6 qualifies

The exact winning law advances UNCHANGED to: 365-day battery →
adversarial controls → force census + tau map → clustered+diffuse
transmission validation → frozen India recession probe →
opinion-causal-receipt retest → only then the canonical chaos/FSLE/
noise-floor remeasurement. Epoch 2 remains forbidden until all pass.

## Permanent engineering ledger

decay_half_life: declared semantics, dead code, meaning NOT
invented. Before Epoch 2: trace its history (commits, design docs,
any prior implementation) and either implement recovered semantics
with tests, or remove the false contract explicitly. A
scientifically meaningful-looking field may not silently do nothing.
