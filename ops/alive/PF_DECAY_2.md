# PF-DECAY-2 — LOOP TOPOLOGY RESTORATION (pre-registered)

Founder ruling after PF-DECAY-1 (accepted: KA PASS / regression FAIL
/ freeze NOT authorized): the decay equation survived; the loop
topology did not. Governing invariant, machine-enforced:

    D_t = Detect(B_t)   — the trigger substrate BEFORE TransitionRule
                          actuation; a rule's actuation must not
                          become part of the substrate used to detect
                          TransitionRules, for ANY rule (conservative
                          historical interpretation: actuation is
                          invisible to detection generally — f933c59
                          had no rule-specific exception).

Everything from 16919e4 stays frozen (IT6-ALL, k=3, mu=0.05,
relax=0.045, dyadic influence, conviction law, flourishing level-map,
Chronicle, news half_life=10d, cooldown semantics, the recovered
decay law C(t)=A·2^(−Δt/h) with signed behavior, ±0.5 envelope,
h≤0 permanent, 0.01 expiry, strict cooldown boundary, locality
semantics). NO coefficient changes. Contract restoration, not model
search.

## The one implementable topology (registered before results)

The ruling admits exactly one implementation, and it is the
historical one. If residues remain in the target path, relax mixes
actuation into stored forces and every social operator propagates
it; recovering B_t would then require either a second persistently
evolving force state (banned: two-Earth problem) or approximate
subtraction of already-propagated residue (banned explicitly).
That path is the ruling's own STOP case.

f933c59's actual topology dissolves the problem: event residues
NEVER entered stored forces. detect_transitions read raw
civ.forces; `effective_deltas_vectorized` applied the decaying
shift at READ time, at the consumer (question projection). Stored
state was the substrate; actuation was an overlay.

PF-DECAY-2 therefore implements:
1. Stored civ.forces IS B_t. The daily dynamic loop (propagate,
   conviction, feed, contagion, chronicle press, relax, detector)
   runs entirely on stored forces and NEVER consumes residues. The
   PF-DECAY-1 target-path application is REMOVED (that edge was the
   accidental loop; its "target path" translation is superseded by
   this ruling's invariant).
2. A fired (rule, locality) creates the same bounded residue state
   as PF-DECAY-1 (chronicle-held, restart-persistent, cooldown-
   gated, recovered law untouched).
3. `effective_forces(w)` — the READ-TIME overlay view: stored
   forces + summed active residue levels for the agent's locality,
   legacy ±0.5 total clip then [0,1] — is the EXPRESSION view,
   consumed by the readout layer (opinion/observer readouts,
   experiment expression measurement, observatory display), exactly
   as legacy consumers received `effective_deltas_vectorized`.
   The overlay is DERIVED at read time from (stored forces, residue
   set); it is not evolved state; nothing drifts.
4. Provenance separation is therefore total and exact: transition
   antecedent = stored forces; transition actuation = residue set.
   No second world, no subtraction, no approximation.

Registered consequence, stated honestly: under the restored
topology cascade effects are expression-layer content — they change
what people express, report, and how readouts measure them, and
they decay; they do not feed back into the stored psychological
substrate. That IS the f933c59 contract. If self-exciting panic /
contagious riots are wanted later, that is new physics with its own
explicit mechanism and calibration (per ruling).

## Known answers (all PF-DECAY-1 greens retained, re-expressed for
   the restored topology; any required failure ⇒ VOID)

KA1/KA6/KA7/expiry/clip: pure recovered-law battery — unchanged,
    must stay green (law untouched).
KA0  flag-off continuity: bit-identical IT6-ALL @8890 recorded
    metrics (flag-off paths untouched).
KA2' level-not-accumulator, restated: paired worlds, planted
    residue: STORED cohort forces identical to control (Δ = 0
    exactly — the loop never sees the residue); EFFECTIVE-view
    cohort delta equals the clipped analytic level L(t) exactly
    (1e-12) — no relax lag: the overlay IS the level. The planted
    integrator (F += L daily) through the same instrument must
    still be DETECTED (stored Δ ≠ 0 immediately).
KA3/KA4 cooldown boundary + locality independence: unchanged
    (detection machinery untouched; clamps act on stored forces).
KA5  restart: canonical save/load mid-decay ⇒ identical residue
    state, cooldown state, stored trajectory, and effective view.
KA8  SELF-REARM PROHIBITION (decisive): base FEAR below trigger;
    plant a panic residue large enough that EFFECTIVE fear crosses
    0.5. Panic must NOT fire — the only crossing came from
    actuation. By construction under this topology; measured
    anyway.
KA9  genuine trigger still works: same residue configuration, and
    independently raise the STORED substrate above threshold ⇒
    panic fires normally, subject to cooldown (cascades were not
    simply disabled).
KA10 detector invariance (the permanent semantic test): two worlds
    identical except one carries active residue ⇒ identical
    non-cascade substrate ⇒ Detect_A = Detect_B exactly, and the
    stored trajectories remain bitwise identical.
KA11 causally visible downstream: after a legitimate firing the
    EFFECTIVE view changes by the residue level, human readouts
    (observer stance on effective view) change, decay occurs on
    schedule, and the change is measurable until expiry. Removing
    one feedback edge does not delete the event.

## Chronicle isolation continuity arm (IT12 premise, prospective)

IT12 stays PASS @16919e4. New arm: TRANSITION_RULES explicitly
empty, same IT12 memory exposure (COMPOSITE and INTRINSIC, h=10d,
seed 8904, same expression measurement). Gate: COMPOSITE normalized
day-30 expression residual in the frozen [0.2, 0.6] band and
INTRINSIC carrier decay analytic-exact. Survives ⇒ the accepted
Chronicle result stands under provably cascade-free isolation.
Fails ⇒ STOP (attribution was entangled); 10d is NOT retuned.

## Regression rerun — the exact world that broke PF-DECAY-1

Seed 8905, same candidate, amplitudes, half-lives, cooldowns,
horizons. Arms:
R4″ natural world 120d, residue flag on (open-loop) vs flag off,
    and vs rules-off identity pair. Track: unique (rule, locality)
    firings; refires and the cause of every refire (under the
    restored topology every firing is substrate-caused BY
    CONSTRUCTION; the receipt is KA10 run against the natural
    world: wiping residues daily must not change the firing
    sequence); active residue count; superposition; saturation
    trajectory measured BOTH on stored forces and on the effective
    view, reported separately; all 8 forces; conviction; diversity
    (sdr); transmission rings; unanimity.
R3″ sustained-trigger stress (clamp days 60–180, 210d horizon):
    firing cadence at the cooldown; residue envelope bounded by the
    analytic superposition; stored-force health gates all pass;
    effective-view elevation local, bounded, decaying after the
    trigger ends.
R2″ single engineered firing: stored-force world-health gates (the
    frozen IT6 set); effective-view cohort expression follows the
    analytic decay exactly.
REQUIRED: N_fires_caused_only_by_actuation = 0, evidenced by the
paired wiped-residue detection control, not asserted.

## Saturation ledger (per ruling; no retroactive rescoring)

The seed-8905 incumbent observation (sat 0.228–0.230, days 100–110,
recovering to 0.191) STAYS on the ledger as development/adversarial
evidence; 8905 can never be presented as held-out confirmation. The
historical IT6 gate meaning (panel samples every 10d, all sampled
panels < 0.20) is preserved for historical results. For the coming
full acceptance battery BOTH are prospectively registered:
sat_terminal AND max_t sat(t) (10d sampling) — a civilization that
rails for 30 days and recovers is not automatically healthy.

## Decision (frozen)

Any required KA fails ⇒ VOID (implementation does not express the
ruled topology; fix implementation only).
Open-loop impossible without an inconsistent second force ontology
⇒ STOP (architectural conflict to founder — registered analysis
above says this does not arise: the overlay is derived, not
evolved).
KAs pass but the natural open-loop world still rails on STORED
forces ⇒ FAIL, stop, no tuning — the residues-in-force-target
question returns to the founder (this registration's topology keeps
residues OUT of stored forces, so stored-force railing would
implicate the base candidate, not the contract).
KAs pass + IT12 isolation survives + natural regression healthy ⇒
PF-DECAY-2 PASS ⇒ FREEZE COMPLETE CANDIDATE (and only then).
