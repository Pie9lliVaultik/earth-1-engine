# 0.8 IT8 — DOSE/PULL INTERSECTION TEST (pre-registered)

IT7 (3b96c0c) accepted: 0 qualify; the stop condition produced the
governing hypothesis — diversity tracks social-dose/pull RATIO,
persistence tracks absolute pull. IT8 tests the two evidence-derived
intersection points. IT7 untouched.

## Candidates (frozen; no third point, no post-result adjustment)

A: k=1, μ=0.05,  relax=0.01  (nominal dose 0.05/day)
B: k=3, μ=0.0167, relax=0.01 (nominal dose 0.0501/day)

Same nominal budget, different scheduling — IT8 also tests whether
dose alone predicts behavior or encounter frequency matters
independently. They are NOT assumed equivalent. Everything else is
the exact IT6/IT7 corrected system (dyadic conviction gain 0.003,
flourishing level-map, cascade cooldown, feed dyadic at the arm's μ,
contagion ambient off, 200k, 120d + day-90 fork, clustered seeding,
unchanged acceptance thresholds).

## Fresh evaluation seeds

The candidate values derive from IT7 data; scored candidate runs use
FRESH world seed 8901 (never used in the ratio-law fit). REF_r045
re-runs at the original seed 8890 for instrument continuity
(unscored). The KA battery runs at 8901 and must discriminate on the
fresh seed.

## Realized-dose instrumentation (per candidate, daily)

actual encounters/person; realized |influence| per person;
cumulative signed social dose/person; encounter-distance
distribution; force variance before/after the social pass; pull
contribution |relax·(target−f)|; the executed dose/pull ratio. The
fitted relationship must survive in EXECUTED physics, not just k·μ.

## Acceptance — completely unchanged

tau [5,15] + resid [0.2,0.6]; clustered ring1 [0.006,0.15],
ring2 ≥ 0.0005, ring3 > 0, no instant consensus; sat < 20% every
channel; sdr ≥ 0.5; unanimity < 50%; α interior/heterogeneous;
designed disagreement softens, designed agreement hardens,
mean-field destroys the signal (KAdis set); full KA battery
discriminates or VOID.

## Selection (registered before execution)

One qualifies → advances. Both qualify → score = |resid − 0.4|
primary (lower wins), clustered ring-1 secondary (higher wins); if
|residA − residB| < 0.05 and both pass, BOTH advance to validation
(no invented preference). Neither qualifies → STOP; no nearby
points; diagnose which dose/pull-model assumption failed (nominal ≠
realized dose; frequency matters separately; transmission needs
more dose than diversity tolerates; pull nonlinearity; another
binding writer).

## If IT8 qualifies

Freeze the winning system immediately — no more force-physics
tuning — and advance unchanged to: 365-day endurance; adversarial/KA
battery; 8-force census; tau map; diffuse+clustered+single-source
transmission; frozen India recession rerun; opinion causal-receipt;
canonical butterfly/FSLE/noise-floor. Only that chain confers
Epoch-2 eligibility. Production untouched.
