# 0.8 RESIDUAL PROBES — consolidated report (founder's locked sequence)

All three probes complete. Artifacts: data/writer_attribution,
data/transmission_probe, data/disagreement_probe; repair + controls
in tests/test_cascade_cooldown.py.

## Probe 1 — fifth writer: CLOSED (contradiction found and repaired)

Cascade rules declare cooldown_days (14–60d) and decay_half_life;
the implementation read neither. collective_surge fired daily in any
chronically-hot locality: a −0.10 event became a −0.10/day grinder
(measured −0.0060/day IDENTITY, −0.0053/day TEMPERAMENT; accounting
closes). feedback.py exonerated (quarantined-legacy only).
REPAIR (authorized, flag-gated EARTH1_CASCADE_COOLDOWN=1, incumbent
bit-exact when off): cooldown honored per (rule, locality); state on
the chronicle, restart-persistent through the canonical serializer.
All six contract controls pass. decay_half_life recorded as a second
unresolved contradiction (semantics ambiguous; nothing invented).

## Probe 2 — transmission equilibrium: CLOSED (no bug; equation
validated; the test's seeding was the hidden assumption)

First-principles receiver equation x* ≈ 0.77·s_eff·S̄ (dose vs pull,
source persistence S̄) predicts the observed 0.0018 and every arm:

| arm | ring1 d30 | prediction confirmed |
|---|---|---|
| ref (random seeds, relax .045) | 0.00181 | reproduces IT5 exactly |
| slow (relax .02) | 0.00428 | persistence is a lever |
| clustered seeds | **0.00740** | **above the 0.006 floor** |
| clustered + slow | **0.01836** | ring-3 spread 0.0012 |
| no-pull control | 0.00782 | the pull erases in transit |

Encounter bookkeeping: ring-1 receivers meet a seeded partner ~0.31
times/day (tie-weighting doubles the naive rate). VERDICT: real
model constraint, not a bug. The registered floor stands and is
REACHABLE — the probe's diffuse random seeding was the unexamined
assumption; real information arrives socially clustered. Recommend:
the next experiment's transmission test registers CLUSTERED seeding
(criterion untouched).

## Probe 3 — softening: capability PERFECT, prevalence ZERO — and
the deeper construct defect

Capability (law unchanged, drive overridden for cohorts):
P(soften | −drive) = 0.9997 at every strength, dose-responsive
E[Δα] (−0.0054 at −0.25, −0.0105 at −0.5 over 30d), symmetric
hardening at +0.5, control ratchets slowly. The C3 law softens
exactly as designed.
Prevalence: natural drive mean **+0.89**, 5th percentile +0.80,
**fraction negative = 0.0000 of 200k**. Nobody ever experiences
net-negative drive.
DEEPER DIAGNOSIS: the drive construct — agreement as distance to the
NEIGHBORHOOD MEAN — cannot register disagreement in any locally
converged field: an agent amid a 50/50 polarized split has nbmean ≈
their own value ⇒ "perfect agreement" amid maximal conflict. This is
the SAME mean-field pathology probe 2 found for transmission,
appearing in the conviction input. The passive ≥10% criterion
measured disagreement prevalence under a construct that erases
disagreement by construction.
Per the pre-authorized path: IT5 remains failed under its frozen
criterion; the criterion is documented INSTRUMENT-INVALID for future
use; the registered replacement (for founder ruling, before the next
experiment): DYADIC drive — conviction responds to agreement/
disagreement with the actual encounter partner (the same dyadic
encounters that carry influence), with a prevalence-informed
criterion derived from measured encounter-level disagreement rates.
No retroactive pass of anything.

## The unification, and the proposed next joint candidate

One structural insight now explains three findings: MEAN-FIELD
AGGREGATION ERASES INDIVIDUAL SIGNAL — it diluted minority
transmission (IT3), and it erases disagreement exposure (probe 3),
while dyadic encounters restore both. The next joint candidate
(freeze after ruling): all3 + cascade-cooldown repair + dyadic-drive
conviction + clustered-seeding transmission protocol + re-registered
softening criterion — with the full ablation matrix and the standing
KA battery, then (if it qualifies) the 365-day battery, adversarial
controls, and the frozen India probe retest.
