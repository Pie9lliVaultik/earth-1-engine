# B2-c1 — external religiosity national input (XI.A.2)

**Opened/closed:** 2026-09-01 · **Status:** ITERATING (gate not met; adoption withheld)

## RESULT (registered MISS)
Religion/values is the worst-losing A-family (Q170 +7.8pp, Q167 +7.5, Q166 +5.5 vs
region-copy) while being the model's best-ORDERED family (cross-country corr +0.60):
the model lacks the country-level religiosity LEVEL. Prior art: WVS-derived
religiosity injection (2026-08-18) produced a fake 1.2pp gain (corr 0.983 with its
own target) and was banned.

## IMPLEMENTATION
- `data/national_inputs/religiosity_factbook.json`: CIA World Factbook (public
  domain, clone c8cfe21), 189/194 countries, per-country verbatim source notes,
  9 documented absents, 0 Pew-derived entries (exclusion rule armed, empty),
  sha 6bc11db9. Rule: religiosity = 1 − explicit nonreligious share; implicit 0
  only when listed shares ≥90%; else ABSENT.
- Instrument change (own row): adjacency gate criterion → PROVENANCE (shared
  respondents/instrument/derivation = BAN; correlation = WARNING; undeclared fails
  closed; verdict sha-bound to the cleared file). External file: CLEAN with
  warn max|corr| 0.766 recorded. All 9 WVS-derived priors: BANNED.
- Genesis: `EARTH1_RELIGIOSITY_FILE` (default legacy, untouched). BITWISE ASSERT
  PASS: flag-on differs only in civ.religiosity (separate rng stream, no consumers).
- Wiring decision (registered): family-scoped consumption — the religiosity column
  enters an item's design matrix only for religion/values items
  (`SB1_SCOPED_FEATURES`), so no other family can regress by construction. The
  unscoped run measured the noise cost this avoids (+0.10 extended, +0.17 pew
  pooled from one extra ridge column).

## RETEST (3× 200k feature worlds, same scorer)
| scope | before | after | Δ |
|---|---|---|---|
| religion family, wvs_heldout (1,872 cells) | 12.16 | 11.72 | **−0.44** (~1.5σ) |
| religion family, goqa/pew (1,617 cells) | 9.15 | 9.35 | +0.20 (<1σ) |
| religion family, POOLED (3,489 cells) | 10.765 | 10.622 | **−0.142** |
| pooled estates (scoped) | 11.84/11.20/12.01 | 11.80/11.20(bitwise)/12.02 | −0.045/0/+0.009 |

## VERDICT
**Gate (≥0.3pp pooled religion-family) NOT MET (−0.142). Adoption withheld; flag
stays off.** The WVS-only signal (−0.44) is real-looking but the pooled gate is the
registered bar. Named unblockers, each its own change: (1) codebook texts — the 110
extended items are family-unclassifiable, so an unknown number of religion items sit
outside the measurement; (2) the family classifier lumps belief items (where the
input helps, −0.44) with politics-of-religion items (where it doesn't, +0.20) —
splitting them is a registered classifier change, not a quiet re-scope.
