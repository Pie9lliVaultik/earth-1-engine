# BENCHMARK A — PHASE 0 INTEGRITY BLOCKERS (closed 2026-08-23)

1. **Public holdout hash** — `benchmarks/cascade_public/holdout_v1.sha256` now holds the FILE sha256 in `sha256sum -c` format (verified: `shasum -a 256 -c` → OK); the LIST hash (sha256 of `json.dumps(sorted_iso2_list)`, default separators, no indent, UTF-8) is recorded in the manifest and prereg text; `scripts/cascade_benchmark_manifest_check.py` verifies both (MANIFEST CONSISTENT).
2. **Environment pin** — `pyproject.toml [repro] = numpy==2.0.2, scipy==1.13.1`; `tests/test_perf_equivalence.py` records PINNED_ENV (CPython 3.9, arm64, Apple Accelerate BLAS) and gates the bit-exact assertion on it, skipping with a loud warning elsewhere (prime: numpy 2.5.2 / scipy-openblas / x86 → skip, not false certification).
3. **Ground-truth estate** — `goqa_ground_truth.json` == `goqa_ground_truth_corrected.json` (sha 5064c397…); every calibration consumer reads that file. **Reproduction from the official WVS-7 v6.0 microdata** (sha recorded in `data/benchmark/goqa_ground_truth_verification.json`): 38/40 items reproduce EXACTLY under an explicit per-item coding (`data/benchmark/goqa_gt_rule_diagnosis.json`: 1–10 scales → ≥6 or ≥7 by item; 1–4/1–5 → top-2 or bottom-2 by polarity; Q57 = 1; Q240 ≤ 4) but **UNWEIGHTED** (design weights W_WEIGHT not applied); Q65 and Q222 (the two VNF "polarity-corrected" items) reproduce under no rule (mean |diff| 0.19 / 0.18). Consequence for Benchmark A: targets are REBUILT from microdata with the per-item rule table AND design weights (Bible §11: "with survey weights"); Q65/Q222 take their explicit scale rule (recorded as a correction of irreproducible cells); the old file is archived as the unweighted reference. The MRP baseline is re-fit on the rebuilt targets through the frozen scorer.
4. **Feature-adjacency gate** (`scripts/feature_adjacency_gate.py`, archived `data/feature_adjacency.json`): BANNED religiosity (Q164 on-benchmark, |corr| 0.983), ideology (Q240 on-benchmark, 0.783), marital, household_size, children, immigrant (broad adjacency); clean: employed, social_class, town_size, income_scale. Active injected set: none. Fail-closed honoured.
