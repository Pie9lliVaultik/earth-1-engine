# PHASE 0.8 ACCEPTANCE BATTERY — MASTER REGISTRATION (frozen)

Authorized by founder ruling after PF-DECAY-2 acceptance. Candidate
executable: 1ae8740 (see FREEZE_COMPLETE_CANDIDATE_0_8.md). Standing
declaration: NO MORE LOCAL PARAMETER OR MECHANISM SEARCH. The
question changes permanently from "what should Earth-1 be?" to "is
what we built actually defensible?"

This master registration freezes the STAGE ORDER, the scored-gate
philosophy, and the decision discipline. Each stage receives its own
sub-registration (instruments, seeds where not fixed here, analytic
expectations) committed BEFORE that stage runs; a sub-registration
may never alter this master's order, gates, or interpretation rules.

## Decision discipline (every stage)

- Required KA / instrument failure ⇒ VOID: repair the instrument
  only; rerun from the frozen registration; defective runs preserved
  and labeled INSTRUMENT-DEFECTIVE / NON-SCORABLE, never FAIL.
- Valid instrument + frozen acceptance gate missed ⇒ FAIL.
- A failure that falsifies a frozen hypothesis ⇒ STOP AND DIAGNOSE;
  no nearby coefficient search; reopening mechanism work requires an
  explicit founder ruling naming the falsified hypothesis.
- Complete frozen chain passes ⇒ PHASE 0.8 PASS ⇒ EPOCH 2 ELIGIBLE
  (a separate explicit initialization ruling still follows).
- Post-hoc arms are always labeled POST-HOC DIAGNOSTIC and may never
  rescue a missed preregistered gate.

## Two health families (permanent)

- STORED-STATE health: the frozen force-health criteria (sat < 20%
  every channel, sdr ≥ 0.5, unanimity < 50%, α interior, no
  railing), now scored on BOTH sat_terminal AND max_t sat(t)
  (10-day sampling) — the seed-8905 lesson: terminal-only health is
  insufficient.
- EFFECTIVE-EXPRESSION health: measured characterization of
  Sat_effective (peak, terminal, days-saturated fraction, earthlings
  affected, duration, channel, geography, rule attribution,
  superposition, clip occupancy, post-expiry recovery). NO numerical
  gate is invented from the observed 0.64; if a hard gate is ever
  needed it must be derived independently from semantics/empirical
  evidence and preregistered before scoring fresh runs. Seed 8905
  and the 0.64 observation are development evidence, never held out.

## Stage A — 365-day endogenous endurance

Fresh preregistered seeds: 9001, 9002, 9003 (never used anywhere).
The frozen candidate, no artificial news/event stream; endogenous
physics only (births, deaths, firms, cascades, chronicle as the
frozen world produces them). Continuous measurement every 10 days:
both health families; all 8 force distributions (stored AND
effective); conviction; sdr/diversity; consensus/unanimity; cascade
firing counts and active residues; chronicle census; employment /
firm health / material stability; population; clamp occupancy
(stored at-bound fraction per channel; overlay ±0.5 clip
engagement) — per the D1 ruling the [0,1]/±0.5 clips must be a
safety envelope, not load-bearing stability.
SCORED GATES (stored family, all three seeds): sat_terminal < 0.20
AND max_t sat(t) < 0.20 on every channel; sdr ≥ 0.5; unanimity
< 50%; α interior (gt99 < 1%, floor < 1%); no population collapse
(alive within demographic plausibility, reported); no monotone
runaway in any channel mean (|Δmean| day 100→365 < 0.15, the
standing no-news stability class). Effective family: measured,
characterized, unscored (first census).

## Stage B — adversarial battery

Deliberately broken implementations must fail VISIBLY: planted
consensus; zero influence; excessive reversion; absent persistence;
conviction ratchet; event accumulator; self-rearming cascade
(closed-loop reintroduced deliberately); cascade→cascade
contamination; effective-view mutation attempt (must raise);
duplicate causality; clamp stress; restart/serialization mid-run;
time-step/resolution invariance where applicable. Standing Rule 2
governs: an instrument that cannot fail a broken implementation is
not evidence.

## Stage C — complete eight-force + cascade census (D2 lives here)

For every force: F_stored vs F_effective distributions and the
decomposition F_effective − F_stored = C_cascade, by rule, locality,
cohort, time, superposition count, residue age, half-life, and
clipping. Deliverable: the first measured answer to "what does a
naturally living civilization's event-expression layer look like?" —
plus, from it, an independently justified proposal (if warranted)
for an effective-expression gate, to be preregistered before any
scoring use.

## Stage D — complete timescale map

Four constructs, never collapsed: T_force (expression relaxation),
T_memory (Chronicle), T_material (material state), T_cascade
(overlay). REQUIRED: the independent informational-persistence case
for the calibrated news-class 10d — evidence NOT used in IT12's
fit; sub-registration must name the held-out case and its band
before the run. IT12 calibrated; Stage D validates or falsifies.

## Stage E — transmission validation

Fresh seeds (registered in the sub-registration): single-source,
clustered, diffuse; A→B→C; ring1/2/3 bands; agreement hardening;
genuine-disagreement softening; minority-information survival.
Chronicle spread and dyadic transmission remain DISTINCT pathways,
measured separately, never substituting for one another.

## Stage F — frozen India recession probe

The existing intervention rerun UNCHANGED on the frozen candidate.
Required: a complete causal receipt firm shock → employment →
income/buffer → deprivation → mental/social state → F_stored →
opinion. Zero opinion movement passes IF the computed state supports
zero; a dead zone caused by pathological saturation cannot.

## Stage G — opinion causal receipt

Every writer traced; no narrated cause absent from computation.
(The same contract binds the investor Observatory: computed
expression is never presented as computed behavioral consequence,
and no LLM bridges that gap.)

## Stage H — butterfly / FSLE / noise floor / consciousness profile

Measure the frozen civilization as it is. λ > 0, λ = 0, λ < 0 are
all admissible outcomes; nothing is tuned to manufacture chaos. The
measurement wins, not the story.

## Stage I — long-horizon / network invariants

Persistence, restart, lifecycle at the real run horizon;
generational/demographic invariants; remaining predeclared V1
blockers.

## Bookkeeping

All stage artifacts under data/acceptance_0_8/<stage>/; every run
records candidate_executable_commit, flags, seeds, instrument
commit; prime is the execution host; production untouched.
